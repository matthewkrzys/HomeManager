package com.home.manager.expenses.repository;

import com.home.manager.expenses.model.Expenses;

public interface ExpensesRepository extends org.springframework.data.jpa.repository.JpaRepository<Expenses, Long>{
}
